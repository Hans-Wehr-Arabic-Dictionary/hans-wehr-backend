apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hans-wehr-api.fullname" . }}-test-connection"
  labels:
    {{- include "hans-wehr-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hans-wehr-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
